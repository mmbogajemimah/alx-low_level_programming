#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * _strlen - Calculates the length of s string
 * @s: String which length is to be found
 *
 * Return: Nothing
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
