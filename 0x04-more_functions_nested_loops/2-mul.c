#include "main.h"
#include <stdio.h>
/**
 * mul - A function that multiplies two integers
 * @a: First integer passed to the function
 * @b: Second integer passed to the function
 *
 * Return: returns the multiplication value of the two ints
 */
int mul(int a, int b)
{
	int z;

	z = a * b;
	return (z);
}
