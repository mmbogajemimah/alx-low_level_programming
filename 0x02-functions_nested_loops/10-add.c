#include "main.h"
#include <stdio.h>
/**
 * add - Adds Integers
 * @x: Integer one
 * @y: Integer two
 *
 * Return: returns the result of adding the two digits
 */
int add(int x, int y)
{
	int z;

	z = x + y;
	return (z);
}
