#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * _abs - Transforms negative digits to positive
 * @n: Integer provided to the function
 *
 * Return: Returns the absolute value
 */
int _abs(int n)
{
	n = abs(n);
	return (n);
}
