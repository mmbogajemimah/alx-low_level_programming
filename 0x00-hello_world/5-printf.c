#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: Returns 0 when the function executes successfully
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
