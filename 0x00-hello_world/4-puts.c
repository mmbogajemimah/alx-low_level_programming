#include <stdio.h>
#include <string.h>
/**
 * main -Entry point
 *
 * Return: Returns 0 if code executes sucessfully
 */

int main (void)
{
	put("\"Programming is like building a multilingual puzzle");
	return (0);
}
